using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SqueezeCollider : MonoBehaviour
{
    public float speedMulti = 0.3f; //How slow the player moves while squeezing
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
